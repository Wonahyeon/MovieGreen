import styled from "styled-components";

const MovieItemBlock = styled.div`
   background: #aabb93;
   color: #ffffff;
   width: fit-content;
   margin: 0 auto;
`;

function MovieTopItem(params) {
   
   return (
      <MovieItemBlock>
      </MovieItemBlock>
   )
}

export default MovieTopItem;